#include "stdafx.h"
#include <iostream>
#include <stdio.h>
#include <string>
#include <windows.h>
using namespace std;

int correct = 0;
string name;
string egenskaper;
string choice;

void main()
{

	cout << "What is the real name of the famous Grabben?" << endl;
	getline(cin, name);
	if (name == "viktor rehn")
	{

		cout << "Well, congrats. You're not completely retarded, like Grabben." << endl;

		correct = correct + 1;


	}

	else if (name != "viktor rehn")
	{

		cout << "You're worse than Grabben, you should feel bad about yourself." << endl;
	}

	system("pause");
	system("cls");

	cout << "Is Grabben perfect and beautiful?" << endl;
	cout << "Yes or no" << endl;
	cin >> choice;

	if (choice == "yes")
	{
		cout << "Seriously? Have you seen him?" << endl;
	}

	else if (choice == "no")
	{
		cout << "At least it seems your taste is to be trusted." << endl;

		correct = correct + 1;
	}

	else
	{
		cout << "That isn't even an option" << endl;


	}

	system("pause");
	system("cls");

	cout << "Is Grabben a) circumsized or does he have a b) micro penis?" << endl;

	cin >> egenskaper;

	if (egenskaper == "a" || egenskaper == "A")
	
	{
		cout << "Hah, that was actually a trick question!! HE GOT BOTH, HE HAS A CIRCUMSIZED MICRO PENIS!" << endl;
		cout << "I'll give you a point for that one though, not a lot of people actually knows that as no one wants to have sex with him." << endl;

		correct = correct + 1;
	}



	else if (egenskaper == "b" || egenskaper == "B" )
	{
		cout << "Hah, that was actually a trick question!! HE GOT BOTH, HE HAS A CIRCUMSIZED MICRO PENIS!" << endl;
		cout << "I'll give you a point for that one though, not a lot of people actually knows that as no one wants to have sex with him." << endl;

		correct = correct + 1;
	}



	else if (egenskaper == "ab" || egenskaper == "AB")
	{
		cout << "Well you're a fucking genius" << endl;
		correct = correct + 1;
	}

	else cout << "That's not even an option, cunt." << endl;  
	


		

	cout << "Give me a moment to check your results" << endl;

	system("pause");
	system("cls");

	if (correct > 1)
	{
		cout << "You're amazing btw!" << endl;
	}

	else if (correct <= 1)
	{
		cout << "Well, I'm sorry to break it to you." << endl;
		cout << "But you're litteraly the most useless person I've ever met." << endl;
		cout << "YOU" << endl;
		cout << "ARE" << endl;
		cout << "USELESSS!!!" << endl;
	}
	else if (correct == 3)
	{
		cout << "I like you, you are everything Grabben isn't." << endl;
		cout << "You're both beautiful and useful, two traits that Grabben needs to become a decent human being." << endl;
	}

	system("pause");
}
